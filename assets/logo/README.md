# Logo Files

Place the DD's Sweet Shack logo here.

Expected file used by the website:

`public/assets/logo/dds_sweet_shack_logo_transparent.png`

You can replace it with the final logo from the business card. Keep the same filename, or update the path in `src/components/Header.tag.ts`. PNG, JPG, or SVG will work when the path matches the code.
